#include "bind.h"
#include "cursor.h"
#include <stdio.h>

int int main(int argc, char * argv[]) {
    initialize_keymap();
    add_bind(500000, char * _Nonnull seq, void * _Nonnull func)

        return 0;
}
